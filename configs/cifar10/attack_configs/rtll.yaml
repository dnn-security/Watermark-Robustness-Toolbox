create:
  name: rtll_attack
  num_classes: 10

remove:
  name: rtll_removal
  epochs: 10

dataset:
  name: CIFAR10DataLoader
  subset: attacker
  root: ~/.wrt/data
  batch_size: 64
  image_size: 32
  num_workers: 2

true_labels: False
name: rtll
output_dir: outputs/cifar10/attacks/rtll/